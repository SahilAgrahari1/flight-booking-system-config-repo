spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SahilAgrahari1/flight-booking-system-config-repo.git
          clone-on-start: true
          default-label: main
server:
  port: 8888
